import { RedTextDrtDirective } from './red-text-drt.directive';

describe('RedTextDrtDirective', () => {
  it('should create an instance', () => {
    const directive = new RedTextDrtDirective();
    expect(directive).toBeTruthy();
  });
});
